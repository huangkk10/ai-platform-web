# PostgreSQL Database Server 環境變數配置
# 複製此文件為 .env 並修改為實際值
#
# 使用方式：
#   cp .env.example .env
#   nano .env  # 修改配置
#   docker compose up -d

# PostgreSQL 配置
POSTGRES_DB=ai_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123

# 注意：修改密碼後，需要同步更新主專案的 docker-compose.yml
# 主專案位置：10.10.172.127:/home/user/codes/ai-platform-web/docker-compose.yml
# 需要修改的環境變數：DB_PASSWORD
